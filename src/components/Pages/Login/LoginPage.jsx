import LoginForm from "../LoginForm.jsx"

function LoginPage() {
    // state (état, données)
    // Comportements
    //  Affichage(render) 
    return (
        <>
            <LoginForm />
        </>
    )
}

export default LoginPage